name: Deployment

on:
  push:
    branches:
      - production

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: production
    concurrency: production
    steps:
      - name: deploy
        run: echo "Midterm1"
        
    
